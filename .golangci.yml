version: "2"

linters:
  default: standard
  enable:
    - gofmt
    - goimports
    - govet
    - unused
    - staticcheck
  exclusions:
    paths:
      - testdata/

formatters:
  enable:
    - gofmt
    - goimports
